# Base model for SD pipeline
BASE_MODEL=runwayml/stable-diffusion-v1-5

# Run mode: fastapi | serve
MODE=serve

# Ray Serve scaling (used when MODE=serve)
RAY_NUM_REPLICAS=1
RAY_NUM_GPUS_PER_REPLICA=1

# Optional: Hugging Face token for private repos
# HF_TOKEN=hf_xxx
